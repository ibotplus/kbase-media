# default multipart config
spring:
  servlet:
    multipart:
      max-request-size: 50MB
      max-file-size: 50MB
      enabled: true

# default logger config
logging:
  level:
    root: debug
    springfox.documentation: warn
    org.springframework: warn
    org.hibernate: warn
    org.thymeleaf: warn
    org.apache: warn
    org.quartz: warn
    io.swagger: warn
    org.zbus: warn
    sun.rmi: warn
  file: ./stdot.log

# default server config
server:
  port: 8080
  servlet:
    context-path: /converter-api

# default rocketmq config
apache:
  rocketmq:
    consumer:
      # consumer group
      push-consumer: yogurt-consumer
    producer:
      # producer group
      producer-group: yogurt-producer
    # nameserver address
    namesrvAddr: 192.168.255.130:9876

# default user converter config
convert:
  # Whether to enable the asynchronous interface
  enable-async: false
  # default output-folder
  outputFolder: E:\\converter-output\\
  outputFolder-async: E:\\converter-output\\async\\
  video:
    vca:
      default: ffmpeg
      ffmpeg:
        # in windows os, it's Necessary, in unix os, it will load by system environment
        path: D:\\ffmpeg\\bin\\
        toImage:
          fps: 0.2
  audio:
    # default asr tool
    asr:
      default: baidu
      # baidu is 60s/seg ; shhan is 20s/seg
      seg-duration: 60
      #baidu asr config
      baidu:
        appId: 11067243
        apiKey: iDEvPvY4zT9CzFgYKMQY6eAi
        secretKey: Wkeh8gIbB2LrNBtGwuechG8TUkLlB2TY
      xfyun:
        appId: 5ab9e7d5
        secretKey: fea13a3145f35c56bd602fb2c3a817cc
      shhan:
        base-url: http://172.16.8.103:8177/shRecBase/
  image:
    #default ocr tool
    ocr:
      default: youtu
      #tencent youtu ocr tool config
      youtu:
        appId: 10125304
        secretId: AKIDVs45xejwtvmW5SpdkjYGpDUZTIwOp0Hn
        secretKey: a0EHCwgHhgnogMCvUr33uhKl195qSwip
        userId: 1071552744
      # abbyy fineReader engine config
      abbyy:
        path: /opt/ABBYY/FREngine11/Bin
        license: SWTT-1101-1006-4491-7660-4166